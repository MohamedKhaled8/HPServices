# دليل تطبيق التصميم الجديد - Expandable Rows

## 🎯 الهدف
تحويل عرض الطلبات من Modal + Grid Cards إلى Expandable Rows (صفوف قابلة للتوسيع)

---

## 📋 الخطوات المطلوبة

### الخطوة 1: تحديث ملف AdminDashboardPage.tsx

#### 1.1 إضافة State جديد
في السطر 119 تقريباً، بعد `const [showPasswords, setShowPasswords] = useState...`:

```tsx
const [expandedRequests, setExpandedRequests] = useState<Set<string>>(new Set());
```

#### 1.2 حذف useEffect الخاص بـ Modal
احذف الكود التالي (السطور 138-148 تقريباً):

```tsx
// Lock body scroll when a modal is open
useEffect(() => {
  if (selectedRequest || isEditingStudent) {
    document.body.style.overflow = 'hidden';
  } else {
    document.body.style.overflow = 'unset';
  }
  return () => {
    document.body.style.overflow = 'unset';
  };
}, [selectedRequest, isEditingStudent]);
```

#### 1.3 استبدال عرض الطلبات
ابحث عن هذا السطر (حوالي السطر 1377):

```tsx
<div className="requests-grid">
```

واستبدل كل شيء من هناك حتى `</div>` المقابل (حوالي السطر 1472) بالكود الموجود في ملف `NEW_EXPANDABLE_CODE.txt`

---

### الخطوة 2: الملفات الجاهزة

✅ **تم إنشاء الملفات التالية:**

1. **`src/styles/AdminExpandableRows.css`**
   - يحتوي على جميع التنسيقات للتصميم الجديد
   - تم استيراده بالفعل في AdminDashboardPage.tsx

2. **`NEW_EXPANDABLE_CODE.txt`**
   - يحتوي على الكود الكامل للـ JSX الجديد
   - انسخ المحتوى واستبدل به الكود القديم

---

## 🎨 ميزات التصميم الجديد

### ✨ المظهر
- صفوف أفقية نظيفة ومرتبة
- خط منقط بين كل طلب
- ألوان متناسقة واحترافية
- أنيميشن سلس عند التوسيع

### 🎯 الوظائف
- **زر قبول**: أخضر، يظهر على اليسار
- **زر رفض**: أحمر، بجانب زر القبول
- **زر السهم**: للتوسيع/الطي، يدور 180 درجة عند التوسيع
- **التفاصيل**: تظهر عند الضغط على السهم

### 📱 Responsive
- **Desktop**: صف واحد مع جميع العناصر
- **Tablet**: تعديل بسيط في الترتيب
- **Mobile**: عمودي بالكامل، أزرار كبيرة

---

## 🔧 كيفية التطبيق اليدوي

### الطريقة السهلة:

1. **افتح ملف** `src/page/AdminDashboardPage.tsx`

2. **ابحث عن** السطر الذي يحتوي على:
   ```tsx
   <div className="requests-grid">
   ```

3. **احذف** كل شيء من هذا السطر حتى `</div>` المقابل له

4. **افتح ملف** `NEW_EXPANDABLE_CODE.txt`

5. **انسخ** كل المحتوى

6. **الصق** مكان الكود المحذوف

7. **احفظ** الملف (Ctrl+S)

8. **حدّث** المتصفح (Ctrl+Shift+R)

---

## 📝 ملاحظات مهمة

### ⚠️ تأكد من:
- ✅ تم استيراد `AdminExpandableRows.css` في أعلى الملف
- ✅ تم إضافة state `expandedRequests`
- ✅ تم حذف useEffect الخاص بـ modal scroll lock
- ✅ تم استبدال الكود القديم بالكامل

### 🎯 النتيجة المتوقعة:
- لا modal بعد الآن
- الطلبات تظهر كصفوف
- كل صف يحتوي على: اسم المستخدم + البريد + التاريخ + الحالة + أزرار
- عند الضغط على السهم، تظهر التفاصيل الكاملة
- خط منقط بين كل طلب

---

## 🐛 استكشاف الأخطاء

### المشكلة: الكود لا يعمل
**الحل:**
1. تحقق من console للأخطاء (F12)
2. تأكد من أن جميع الأقواس متطابقة
3. تأكد من استيراد CSS الجديد

### المشكلة: التنسيق غير صحيح
**الحل:**
1. امسح الكاش (Ctrl+Shift+Delete)
2. حدّث الصفحة (Ctrl+Shift+R)
3. تحقق من أن ملف CSS موجود

### المشكلة: الأزرار لا تعمل
**الحل:**
1. تحقق من أن `expandedRequests` state موجود
2. تحقق من دالة `toggleExpand` داخل map
3. راجع console للأخطاء

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الملف بعناية
2. تحقق من ملف `NEW_EXPANDABLE_CODE.txt`
3. راجع ملف `AdminExpandableRows.css`
4. افحص console للأخطاء

---

## ✅ قائمة التحقق النهائية

قبل الاختبار، تأكد من:
- [ ] تم إضافة `expandedRequests` state
- [ ] تم حذف useEffect للـ modal
- [ ] تم استبدال `requests-grid` بـ `requests-list-container`
- [ ] تم استيراد `AdminExpandableRows.css`
- [ ] تم حفظ جميع الملفات
- [ ] تم تحديث المتصفح

---

**بالتوفيق! 🚀**

آخر تحديث: 2026-01-31
