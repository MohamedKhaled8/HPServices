# حل مشكلة "الأذونات مفقودة أو غير كافية"

## المشكلة:
عند تسجيل الدخول تظهر رسالة "الأذونات مفقودة أو غير كافية"

## الحلول:

### ✅ الحل 1: تفعيل Email/Password Authentication

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك `hpservices-29a44`
3. من القائمة الجانبية، اضغط على **"Authentication"** أو **"المصادقة"**
4. إذا كانت المرة الأولى:
   - اضغط **"Get started"** أو **"ابدأ"**
5. اذهب إلى تبويب **"Sign-in method"** أو **"طرق تسجيل الدخول"**
6. اضغط على **"Email/Password"**
7. فعّل **"Enable"** أو **"تفعيل"** (يجب أن يكون مفعّل)
8. اضغط **"Save"** أو **"حفظ"**

### ✅ الحل 2: إعداد Firestore Security Rules

1. في Firebase Console، اذهب إلى **"Firestore Database"**
2. اضغط على تبويب **"Rules"** أو **"القواعد"**
3. استبدل القواعد الحالية بهذا الكود:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قاعدة بيانات الطلاب
    match /students/{userId} {
      // السماح للمستخدم بقراءة وكتابة بياناته الخاصة
      allow read, write: if request.auth != null && request.auth.uid == userId;
      // السماح بقراءة جميع البيانات للمستخدمين المسجلين
      allow read: if request.auth != null;
    }
    
    // قاعدة بيانات طلبات الخدمات
    match /serviceRequests/{requestId} {
      // السماح للمستخدم بقراءة وكتابة طلباته الخاصة
      allow read, write: if request.auth != null && request.auth.uid == resource.data.studentId;
      // السماح بإنشاء طلبات جديدة
      allow create: if request.auth != null;
    }
  }
}
```

4. اضغط **"Publish"** أو **"نشر"**

### ⚠️ للاختبار فقط (غير آمن للإنتاج):

إذا أردت السماح بالقراءة والكتابة للجميع (للاختبار فقط):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

**تحذير:** هذه القواعد تسمح للجميع بالوصول. استخدمها فقط للاختبار!

### ✅ الحل 3: التحقق من إنشاء Firestore Database

1. في Firebase Console، اذهب إلى **"Firestore Database"**
2. إذا لم تكن موجودة:
   - اضغط **"Create database"** أو **"إنشاء قاعدة بيانات"**
   - اختر **"Start in test mode"** (للاختبار)
   - اختر موقع قاعدة البيانات
   - اضغط **"Enable"** أو **"تفعيل"**

### ✅ الحل 4: التحقق من البيانات

1. بعد تسجيل مستخدم جديد، تحقق من:
   - **Authentication** → **Users**: يجب أن يظهر المستخدم
   - **Firestore Database** → **students**: يجب أن تظهر بيانات المستخدم

### 🔍 خطوات التشخيص:

1. افتح **Developer Console** في المتصفح (F12)
2. اذهب إلى تبويب **Console**
3. حاول تسجيل الدخول
4. اقرأ رسالة الخطأ بالتفصيل
5. الأخطاء الشائعة:
   - `auth/operation-not-allowed` → Email/Password غير مفعّل
   - `permission-denied` → مشكلة في Firestore Rules
   - `auth/user-not-found` → المستخدم غير موجود

### 📝 ملاحظات مهمة:

- تأكد من تفعيل **Email/Password** في Authentication
- تأكد من إنشاء **Firestore Database**
- تأكد من نشر **Security Rules** بعد تعديلها
- انتظر بضع ثوانٍ بعد التغييرات في Firebase Console

---

## بعد تطبيق الحلول:

1. أعد تحميل الصفحة (F5)
2. جرب تسجيل مستخدم جديد أولاً
3. ثم جرب تسجيل الدخول

إذا استمرت المشكلة، تحقق من رسالة الخطأ في Console وأخبرني بها.

